# Git / GitHub
.git
.gitignore
.github
.distignore
README.md
.DS_Store

# Composer / PHPUnit (development only)
tests
vendor
composer.json
composer.lock
phpunit.xml.dist

# Node (if added later)
node_modules
package.json
package-lock.json

# Cloud Agent & local WordPress dev (GitHub only — not for WordPress.org)
.cursor
.wordpress
scripts

# Plugin directory screenshots/banners live in SVN /assets, not in the trunk zip
assets

*.zip
